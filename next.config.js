const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["res.cloudinary.com"],
    },
    resolve: {
        extensions: [".js", ".jsx", ".ts", ".tsx"],
    },
}

module.exports = nextConfig
